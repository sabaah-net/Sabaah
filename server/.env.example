# Firebase Admin SDK service account key (JSON string or base64-encoded JSON)
# Get this from: Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"sabaah-396a8",...}

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://sabaah-396a8-default-rtdb.asia-southeast1.firebasedatabase.app

# Port the server listens on
PORT=3001

# Optional: API key for authenticating client requests
# API_KEY=your-secret-api-key
