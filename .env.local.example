# Firebase Admin API server URL
# When set, all Firebase DB operations go through the server (bypasses public rules)
# Leave empty to use direct Firebase Web SDK (for development)
NEXT_PUBLIC_FIREBASE_API_URL=

# Optional API key for server authentication
NEXT_PUBLIC_FIREBASE_API_KEY=
